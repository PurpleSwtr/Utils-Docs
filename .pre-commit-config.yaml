# Временно убрал, и вообще сомневаюсь что мне нужно что-то подобное, потому что ломаются все tip'ы.

# repos:
#   - repo: local
#     hooks:
      # - id: my-formatter
      #   name: md formatter
      #   entry: uv run python utils/format_all_md_files.py
      #   language: system
      #   types: [markdown]
      #   pass_filenames: true
      
      # - id: generate-todolist
      #   name: generate todolist
      #   entry: uv run python -m api.table_generator.service
      #   language: system
